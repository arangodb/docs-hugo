program-options: ${PROGRAMOPTIONS_DST}
error-codes: 
  src: ${ARANGODB_MAIN_SRC}/lib/Basics/errors.dat
  dst: ${ERRORCODES_DST}
metrics: 
  src: ${ARANGODB_MAIN_SRC}
  dst: ${METRICS_DST}
apidocs: ${APIDOCS_DST}
servers:
  - name: ${SERVER_NAME}
    image: ${SERVER_BRANCH}
    version: ${SERVER_VERSION}
