{{- $base := .base }}
{{- $page := .page }}
{{- $parameter := .parameter }}
  {{- if (fileExists (printf "/layouts/partials/%s.html" $base)) }}
    {{- partial (printf "%s.html" $base ) $parameter }}
  {{- end }}